| fine_tuning_target | hypothesis | current_evidence | claim_status | next_gate |
| --- | --- | --- | --- | --- |
| Direct SQL SFT | A small model can learn conversational SQL directly from chat-format SQL rows. | Supported proxy: 100-step LoRA reached 0.530 strict accuracy; best non-oracle prompt reached 0.640 value accuracy on the fixed CoSQL slice. | supported_proxy | Run the same target on hosted baselines and BIRD-Interact-style tasks. |
| Planner/DSL first, SQL second | Predicting tables, joins, grain, filters, and projection before SQL should reduce context failures. | Planner F1 is measurable, but predicted-plan SQL execution has not beaten direct SQL yet. | pending | Run predicted-planner endpoint SQL and compare against matching direct SQL manifests. |
| Semantic-layer tuning | Learning governed entities, dimensions, measures, grain, and joins should improve analytical correctness. | Supported proxy: semantic prompt policy reaches 0.640 value accuracy, but broad semantic context is not yet a proven method win. | supported_proxy | Replace derived schema summaries with versioned semantic artifacts and measure deltas on matching rows. |
| MEASURE()-preserving metric DSL | The model should preserve governed metric intent before SQL compilation. | Metric-DSL parse, compile, execution, and measure-preservation gates exist; comparison is still pending. | pending | Evaluate metric-DSL predictions against a direct-SQL baseline on the same metric-heavy rows. |
| Behavior/recovery tuning | A useful agent must clarify, inspect values, repair failures, and recover after its own earlier mistakes. | The shareable lab isolates empty-result repair as a separate behavior; teacher-forced CoSQL history is supported, but generated-history rollout improvement is still pending. | pending | Run model-generated-history rollout and compare it with teacher-forced history for the same model. |
