| rung | experiment | question | required_artifact | claim_gate |
| --- | --- | --- | --- | --- |
| 1 | Direct SQL control | Does ordinary direct-SQL tuning help on the same rows, same local endpoint path, same scorer, and no oracle hints? | direct-SQL endpoint manifest on the fixed CoSQL proxy and, later, the BIRD-Interact transfer rows | This is the control arm. It can support a proxy movement claim, but every structured target must beat it before being called better. |
| 2 | Planner quality before SQL | Can a non-oracle planner recover tables, columns, joins, projection shape, grouping, and duplicate policy before the generator writes SQL? | planner scorecard with row identities, planner labels, and planner-readiness diagnostics | First score the plan before SQL. Do not turn planner movement into an execution claim until the plan is good enough to feed. |
| 3 | Predicted-planner SQL | Do non-oracle predicted plans improve generated SQL against the direct-SQL control on identical rows? | paired direct-vs-predicted endpoint manifest from eval.run_predicted_planner_comparison | The predicted-planner path must beat direct SQL with the same model, rows, scorer, prompt boundary, and oracle policy. |
| 4 | Semantic-layer target | Can the model or retrieval layer recover governed entities, dimensions, measures, grain, joins, and value aliases without flooding the prompt? | versioned semantic artifacts, value/entity retrieval scores, and row-matched SQL deltas | Semantic context must reduce value, entity, and grain errors on the same rows without hiding cost or latency. |
| 5 | MEASURE()-preserving DSL | Can the model preserve MEASURE() intent and compile it through a semantic model before comparing value execution? | metric-DSL prediction manifest with parse, compile, MEASURE() preservation, semantic-model hashes, and compiled SQL execution | Metric DSL must parse, compile, preserve governed measures, and match or beat direct SQL on the same metric-heavy rows. |
| 6 | Generated-history rollout | Can the system continue after its own previous SQL, empty results, and repair attempts instead of reading clean history? | model-generated history rollout manifest with execution results, repair actions, stop reasons, and same-model controls | Generated-history rollout must beat or explain teacher-forced history on the same model and rows before recovery claims count. |
| 7 | Hosted and BIRD-Interact comparison | Can the small specialized local model challenge hosted models on the richer interactive analysis setting? | same-protocol hosted baselines, local-vs-hosted manifest, latency/cost report, and BIRD-Interact transfer manifest | Only compare with hosted SOTA after the local protocol is stable, non-oracle, row-matched, and transferred to BIRD-Interact-style tasks. |
