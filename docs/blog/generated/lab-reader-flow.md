| step | post_section | lab_section | reader_action | evidence_to_inspect | claim_boundary |
| --- | --- | --- | --- | --- | --- |
| 1 | Run the lab | Research question | Open the notebook and start with the research question before looking at scores. | Reader flow table, runtime policy, scenario hash | The lab starts from the same question as the post: model size is not the main variable if the target behavior is wrong. |
| 2 | Why one-shot SQL isn't enough | single-turn gap | Compare the single-turn standalone request with the follow-up turns where the metric, filter, grain, and repair state carry forward. | Questions, context notes, and per-turn behavior trace | single-turn SQL plus chat history is not enough when the user changes filters, grain, values, and recovery state across turns. |
| 3 | Isolating the datasets | proxy slice | Read the tiny SQLite scenario as the local analogue of the fixed CoSQL proxy slice. | Scenario hash, turn count, recovery turn | 100 CoSQL turns are the current proxy for fast iteration; the tiny SQLite lab mirrors the same failure classes without a model download. |
| 4 | Fine-tuning loop | target comparison | Use the method matrix to compare direct SQL, planner-first SQL, semantic grounding, metric DSL, and recovery targets. | Method matrix and target comparison table | The lab compares five fine-tuning targets: direct SQL, planner-first SQL, semantic value grounding, MEASURE()-preserving DSL, and behavior/recovery. |
| 5 | Planning before SQL | execution trace | Inspect where each target succeeds or fails before treating a miss as generic bad SQL. | Value match, context carryover, value grounding, SQL trace | The four-turn SQLite scenario exposes context carryover, value grounding, metric preservation, and recovery as separate behaviors. |
| 6 | Preserving MEASURE() | execution trace | Check which systems keep MEASURE(revenue) in the intermediate state before SQL expansion. | measure_preserved and intermediate_plan columns | MEASURE() preservation is a training target in the lab, not yet a production win. |
| 7 | The road ahead | next gates | Use the next gates to decide what artifact to build next. | non-oracle planner, metric DSL manifest, generated-history rollout, hosted baseline, and BIRD-Interact transfer. | This is not a benchmark result; it is a runnable method comparison that explains what evidence the full repo still needs. |
