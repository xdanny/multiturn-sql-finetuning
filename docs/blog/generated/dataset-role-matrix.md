| data_source | project_role | what_it_tests | why_single_turn_is_not_enough | current_status | next_artifact |
| --- | --- | --- | --- | --- | --- |
| BIRD-Interact | north-star interactive benchmark for the final hosted-SOTA comparison | multi-turn data analysis over richer schemas, values, and interaction state | The agent must carry intent, inspect results, and recover across turns rather than solve one isolated SQL request. | pending transfer target; no local-vs-hosted manifest yet | BIRD-Interact transfer manifest with local model, hosted baselines, cost, latency, and identical scorer settings |
| BIRD mini-dev | single-turn execution-harness check, not a dialogue claim | schema linking and SQL execution on BIRD-style databases | It can validate the SQL runner, but it cannot expose follow-up state, generated-history drift, or recovery behavior. | useful harness input; not promoted as multi-turn evidence | same-runner bridge into BIRD-Interact-style turns |
| CoSQL | current fixed proxy slice for fast multi-turn iteration | dialogue-context SQL over Spider-style SQLite databases with teacher-forced history | The same schema can require filter carryover, value grounding, projection stability, and history alignment across turns. | fixed 100-turn proxy used by the claim ledger | generated-history rollout plus predicted-planner SQL comparison on the same CoSQL rows |
| SParC | context-dependent SQL training/evaluation support | context-dependent follow-up SQL without full conversational interaction pressure | It helps teach follow-up resolution, but still needs explicit value grounding, semantic metric, and recovery labels. | available as a related dataset role, not yet a public result | row-compatible SParC manifest with the same scorer and claim ledger |
| Synthetic schema-rich SQL | schema-rich stress data for joins, grain, fanout, and metrics | long DDL, bridge tables, duplicated children, metric definitions, and awkward value/entity mappings | Synthetic rows can isolate value grounding, join fanout, and metric preservation failures before expensive endpoint runs. | design target for the next data-artifact buildout | fixture pack with value indexes, fanout cases, and MEASURE() labels |
| Tiny SQLite lab | portable notebook demonstration attached to the post | five candidate targets on one four-turn scenario: direct SQL, planner-first, semantic state, metric DSL, and recovery | The tiny lab makes context carryover, value grounding, metric intent, and recovery visible without model serving. | runnable local artifact; not a benchmark result | promote each isolated behavior into a dataset-backed manifest |
