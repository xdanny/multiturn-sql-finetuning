| mechanism | single_turn_assumption | multi_turn_breakage | dataset_surface | repo_artifact_needed |
| --- | --- | --- | --- | --- |
| state carryover | A BIRD-style one-shot request contains the whole task in one question plus schema text. | A follow-up such as 'now by month' depends on the prior metric and filters, but the current utterance no longer states them. | CoSQL fixed proxy and SParC context-dependent turns expose filter and projection carryover. | resolved standalone question, carried filter labels, and planner state deltas before SQL generation |
| value grounding | The question literal is usually usable directly or the model can guess the stored value from schema names. | The user says France while the database stores FR; the France -> FR mapping is missing, so the SQL can execute and still return empty rows. | CoSQL value labels, the database-derived value index, and Synthetic schema-rich SQL alias fixtures. | value index, entity-resolution labels, alias coverage, and value-retrieval accuracy before final SQL |
| grain shift | The requested grouping and projection are stated once and can be compiled directly. | A follow-up changes country totals into monthly totals while the model must preserve the measure and avoid duplicated rows. | CoSQL/SParC follow-ups plus Synthetic schema-rich SQL bridge tables and fanout fixtures. | grain labels, duplicate-row policy, join-path labels, and fanout-safe value checks |
| metric intent | A raw SQL expression is enough because the benchmark asks for one executable query. | Once the user keeps editing the analysis, expanding the metric too early hides whether revenue means MEASURE(revenue), a governed expression, or an ad hoc SUM. | Synthetic schema-rich SQL metric rows now, then metric-heavy CoSQL/SParC/BIRD-Interact transfers. | semantic model manifest, MEASURE() preservation score, DSL parse/compile rate, and compiled-SQL execution delta |
| result-aware recovery | The model is scored on the first answer, so it never has to use an empty result or error as feedback. | If a previous turn returned no rows, the next turn may require repairing the value, inspecting the database, or asking a clarifying question instead of retrying the same SQL. | Tiny SQLite lab for the visible example; Synthetic schema-rich SQL and BIRD-Interact for real repair pressure. | execution-result trace, repair action labels, clarification policy, and recovery-success scoring |
| generated-history drift | There is no history, or evaluation gives the model clean reference history for every turn. | Teacher-forced history hides compounding failures; production agents must continue after their own bad SQL and result state. | CoSQL generated-history rollout first, then BIRD-Interact same-protocol local-vs-hosted runs. | generated-history manifest, same-model teacher-forced control, latency/cost trace, and local-vs-hosted comparison |
