| protocol_gate | required_evidence | why_it_matters | minimum_acceptance | claim_ids |
| --- | --- | --- | --- | --- |
| same input rows | A frozen prepared-input manifest with row IDs, dialog IDs, database hashes, schema text hashes, prompt template hashes, and no model-specific filtering. | A hosted/local comparison is meaningless if the models answer different questions or see different schema context. | The hosted model manifest and local model manifest must cover the same input rows with matching hashes. | hosted_sota_same_protocol, local_beats_hosted_same_protocol |
| same scorer and output schema | One execution scorer version, one value-normalized result schema, scorer hash, and per-row failure classification. | Strict SQL string differences, alias labels, and harmless column names should not decide the hosted/SOTA claim. | Both manifests report strict accuracy, value accuracy, syntax accuracy, and classified failures from the same scorer build. | hosted_sota_same_protocol, local_beats_hosted_same_protocol |
| same oracle boundary | Explicit flags for oracle planner hints, teacher-forced history, schema pruning, semantic artifacts, and value indexes. | Oracle planner diagnostics are useful ceilings, but they cannot be mixed into a production-style hosted comparison. | Any run using labels derived from reference SQL is excluded from local-vs-hosted win claims. | hosted_sota_same_protocol, local_beats_hosted_same_protocol |
| hosted model manifest | Hosted model name, provider, model version/date, prompt hashes, decoding settings, retry policy, raw outputs, parsed SQL, execution results, and per-row latency. | The phrase hosted SOTA is too vague unless the exact models and settings are reproducible. | At least one strong hosted baseline is run through the same pipeline before the local model is compared. | hosted_sota_same_protocol |
| local model manifest | Local base model, adapter hash, training data manifest, checkpoint hash, prompt hash, decoding settings, raw outputs, parsed SQL, execution results, and per-row latency. | A local win should identify the exact specialized model, not only the endpoint or repo branch used for the run. | The local model manifest must show a positive local-vs-hosted delta on value accuracy before the post can claim competition. | local_beats_hosted_same_protocol |
| generated-history rollout | A rollout manifest where each turn consumes the model's own previous SQL and result state, plus a same-model teacher-forced control. | Multi-turn analysis breaks through compounding mistakes; clean history can hide exactly the behavior being evaluated. | Report generated-history value accuracy and recovery delta beside the teacher-forced run. | rollout_beats_teacher_forced_history, bird_interact_local_vs_hosted |
| latency and cost | Per-row latency, total tokens or local throughput, hosted API cost, local hardware, and batch/concurrency settings. | A small local model can be useful even when accuracy is close, but only if latency and cost are measured on the same rows. | The comparison reports accuracy, latency and cost together; none of them can be inferred from model names. | hosted_sota_same_protocol, local_beats_hosted_same_protocol |
| BIRD-Interact transfer | A BIRD-Interact transfer manifest with the same scorer, row identity contract, non-oracle boundary, rollout policy, and hosted/local model manifests. | CoSQL is a fast proxy; the final question is whether the method survives a richer interactive analysis benchmark. | A BIRD-Interact transfer run exists before making broad multi-turn data-analysis claims. | bird_interact_local_vs_hosted |
