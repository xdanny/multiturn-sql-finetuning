| priority | fine_tuning_target | why_now | dataset_focus | success_metric | build_next | falsifies_if | claim_ids |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Planner/DSL first, SQL second | It attacks the oracle gap directly: the best diagnostic result gets much easier when schema linking, joins, projection, and grain are already planned. | CoSQL fixed proxy first, then BIRD-Interact transfer | predicted planner label F1 plus generated SQL value accuracy beating direct SQL on the same rows | Run eval.planner_optimize with DSPy proposals, then use eval.planner_predict as the endpoint harness for the promoted planner program without gold SQL labels. Execute the final direct-vs-predicted pair through eval.run_predicted_planner_comparison. | planner F1 improves but final SQL does not beat direct SQL on the same scorer and oracle policy | planner_lexical_schema_baseline, predicted_planner_sql_execution |
| 2 | Semantic-layer tuning | The current lab and proxy failures show value/entity grounding and grain errors that raw DDL does not explain. | CoSQL plus synthetic value/entity fixtures | value execution accuracy, entity-resolution accuracy, and fanout-safe grain labels on the same rows | versioned semantic artifacts, value/entity indexes, and retrieval or pruning manifests before another broad semantic prompt run | semantic context increases prompt size or latency without reducing value grounding and grain errors | semantic_prompt_minimal_executable_cosql_dev_100turns, hosted_sota_same_protocol |
| 3 | MEASURE()-preserving metric DSL | It is the cleanest way to test whether governed metric intent should be learned before SQL expansion. | synthetic schema-rich SQL and metric-heavy CoSQL/SParC rows | metric_dsl_parse_rate, compile_rate, measure_preservation, and compiled SQL value accuracy versus direct SQL | metric-DSL prediction manifest with the same semantic model version and a row-matched direct-SQL baseline | MEASURE() preservation is high but compiled execution does not match or beat direct SQL | metric_dsl_evaluation_manifest, metric_dsl_beats_direct_sql |
| 4 | Behavior/recovery tuning | A real multi-turn analyst must live with its own earlier SQL and result state, but the current proxy still uses teacher-forced history. | CoSQL generated-history rollout, then BIRD-Interact | model-generated-history value accuracy and recovery-success delta over teacher-forced evaluation | generated-history rollout manifest with empty-result repair, clarification, and retry labels | the model only works with clean teacher-forced history and collapses after its own first wrong turn | model_generated_history_rollout, rollout_beats_teacher_forced_history |
| 5 | Direct SQL SFT | It remains the control arm, not the most interesting next bet; every structured method must beat it before claiming improvement. | fixed CoSQL proxy, hosted baselines, and BIRD-Interact | strict and value execution accuracy under the same scorer, rows, prompt boundary, and oracle policy | same-protocol hosted baseline, local-vs-hosted comparison, and BIRD-Interact transfer so the direct-SQL control is not just a local proxy number | a more structured target cannot beat the direct-SQL control on identical rows | qwen35_9b_base_cosql_dev_100turns, multiturn_sql_100_cosql_dev_100turns, hosted_sota_same_protocol, local_beats_hosted_same_protocol, bird_interact_local_vs_hosted |
