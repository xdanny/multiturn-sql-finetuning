| optimization_scope | source_artifact | best_variant | best_source | best_accuracy | best_dspy_accuracy | samples | promoted_decision | next_program_target | claim_boundary |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| non_oracle_sql_prompt_smoke | results/prompt_search_semantic50_limit30/summary.csv | semantic_grounding | static | 0.400 | 0.400 | 30 | tie: DSPy matched the best static prompt on a 30-row smoke run, so it is useful as a harness but not a promotion by itself. | Run prompt search on a held-out proxy slice and score failure-taxonomy deltas. | Prompt search smoke evidence only; not a SOTA claim. |
| oracle_schema_pruned_prompt_search | results/prompt_search_schema_pruned_projection_schemafix_100/summary.csv | schema_pruned_minimal | schema_pruned_static | 0.850 | 0.830 | 100 | static: the concise schema_pruned_minimal contract beat the best DSPy wording, so longer final-SQL prompts are not the next bet. | Use DSPy to optimize the planner contract, not only final SQL wording. | Oracle diagnostic evidence only; not a SOTA claim. |
| planner_program_optimization_gate | docs/planner_baseline_cosql_dev_100_summary.json | not_run | pending |  |  | 0 | harness_ready: eval.planner_optimize can score static and DSPy planner policies by planner F1 before SQL generation. | Run eval.planner_optimize with DSPy proposals, promote the best planner policy into eval.planner_predict, then use eval.run_predicted_planner_comparison to run direct SQL and predicted-planner SQL with the same model, rows, scorer, and oracle policy. | Planner optimizer exists, but no planner-search summary is published yet; not a SOTA claim. |
