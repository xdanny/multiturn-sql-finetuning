| fine_tuning_target | control_arm | win_condition | required_comparison | current_blocker | claim_ids |
| --- | --- | --- | --- | --- | --- |
| Direct SQL SFT | Base Qwen and 100-step direct-SQL LoRA | Defines the baseline every structured target must beat; not enough for a hosted-SOTA claim by itself. | Compare on same rows, same scorer, same prompt boundary, and same oracle policy before reporting movement. | Hosted same-protocol baseline, a positive local-vs-hosted delta, and BIRD-Interact transfer are pending, so direct-SQL movement is only proxy evidence. | qwen35_9b_base_cosql_dev_100turns, multiturn_sql_100_cosql_dev_100turns, hosted_sota_same_protocol, local_beats_hosted_same_protocol, bird_interact_local_vs_hosted |
| Planner/DSL first, SQL second | Direct SQL SFT on the fixed proxy slice | A non-oracle planner must recover tables, joins, projection shape, grain, duplicate policy, and enough values for SQL generation to beat direct SQL execution. | Compare predicted plans and generated SQL on same rows, same scorer, same schema text, and no labels extracted from gold SQL. | non-oracle planner-to-SQL execution is pending; current oracle planner rows are ceilings, not production evidence. eval.run_predicted_planner_comparison is the required paired endpoint runner for the next manifest. | planner_lexical_schema_baseline, predicted_planner_sql_execution |
| Semantic-layer tuning | Direct SQL prompt plus unpruned schema context | A semantic-state target must improve value grounding, entity resolution, and grain choices without flooding the prompt. | Compare semantic artifacts and final SQL on same rows, same scorer, same latency/cost accounting, and versioned semantic model snapshots. | Value index, entity-resolution labels, and versioned semantic model manifests are not complete. | semantic_prompt_minimal_executable_cosql_dev_100turns, hosted_sota_same_protocol, local_beats_hosted_same_protocol |
| MEASURE()-preserving metric DSL | Direct SQL SFT on metric-heavy rows | The model must preserve governed metric intent as MEASURE() until compilation, then match or beat direct SQL value accuracy. | Compare DSL parse, compile, measure preservation, and execution on same rows, same scorer, same semantic model version, and the same direct-SQL baseline. | metric-DSL prediction manifest and direct-SQL delta are pending. | metric_dsl_evaluation_manifest, metric_dsl_beats_direct_sql |
| Behavior/recovery tuning | Teacher-forced history and direct SQL retry behavior | The model must recover from its own prior empty results or bad SQL, not only answer when the history is clean. | Compare teacher-forced and generated-history rollouts on same rows, same scorer, same model, and same stopping rules. | generated-history rollout and recovery delta are pending. | model_generated_history_rollout, rollout_beats_teacher_forced_history |
